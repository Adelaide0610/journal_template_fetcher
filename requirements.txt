# ============================================================
# 核心依赖（必须安装）
# ============================================================
requests>=2.31.0
beautifulsoup4>=4.12.0

# ============================================================
# 搜索引擎增强（可选，根据需要安装）
# ============================================================
# 方案 A：Google 搜索结果抓取（零配置，免费但有验证码风险）
# pip install googlesearch-python
# 方案 B：Google Custom Search API（最稳定，需要 API Key + CX）
#   无需额外包，直接设置环境变量:
#   set GOOGLE_API_KEY=xxx
#   set GOOGLE_CX=xxx

# ============================================================
# 浏览器渲染（可选，解决 JS 动态页面问题）
# ============================================================
# pip install playwright
# playwright install chromium
#
# 安装后，使用 --browser 参数启用:
#   python -m journal_template_fetcher "Nature" --browser

# ============================================================
# DeepSeek AI 兜底（可选）
# ============================================================
# 设置环境变量或使用 --api-key 参数:
#   set DEEPSEEK_API_KEY=sk-xxx
#   python -m journal_template_fetcher "xxx" --deepseek
